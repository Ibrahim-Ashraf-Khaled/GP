# 🚀 تقرير جاهزية الإنتاج (Production Readiness Report)
**المشروع:** عقارات جمصة (gamasa-properties)
**تاريخ التقرير:** 20 فبراير 2026
**الحالة العامة:** ⚠️ غير جاهز للإنتاج

---

## 1. إدارة البيئة (Environment Management)

### 🔍 تحليل الوضع الحالي

**الملف الفعلي: `.env.local`**
```env
NEXT_PUBLIC_SUPABASE_URL=https://trrbabfexmjjqiwsdkji.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
NEXT_PUBLIC_IS_MOCK_MODE=true   ← ❌ مضبوطة على true دائماً
```

**عدد مرات استخدام `IS_MOCK_MODE` في الكود:**
| الملف | عدد الاستخدامات |
|-------|----------------|
| `src/services/supabaseService.ts` | **45+ مرة** |
| `src/context/AuthContext.tsx` | 5 مرات |
| `src/lib/storage.ts` | 1 مرة |
| **المجموع** | **أكثر من 50 شرط if** |

### ⚠️ المشكلات المكتشفة

#### 🔴 مشكلة 1: المشروع عالق في وضع Mock
الملف `.env.example` نفسه يضبط `NEXT_PUBLIC_IS_MOCK_MODE=true` كقيمة **افتراضية**، مما يعني أن أي مطوّر جديد يستنسخ المشروع سيشغّل نسخة وهمية من البيانات دون أن يدرك ذلك.

```env
# .env.example (الرسمي!)
NEXT_PUBLIC_IS_MOCK_MODE=true  ← ❌ يجب أن تكون false كافتراضي آمن
```

#### 🔴 مشكلة 2: مفتاح Supabase الحقيقي مكشوف في `.env.local`
رغم أن `.env.local` في `.gitignore`، إلا أنه يحتوي على:
- **مفتاح Anon key حقيقي** يمكن أي شخص يستخدمه الوصول لقاعدة البيانات مباشرةً من المتصفح
- لا يوجد فصل بين بيئة **التطوير** و**الإنتاج** (نفس المشروع Supabase للاثنتين)

#### 🟠 مشكلة 3: غياب متغيرات البيئة الحرجة
المتغير `NEXT_PUBLIC_SITE_URL` مستخدم في `layout.tsx` لكنه غير موجود في `.env.example` أو `.env.local`:
```typescript
// layout.tsx - السطر 21
metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL || 'https://gamasa-properties.vercel.app'),
```

كما يوجد `YOUR_VERIFICATION_CODE` مكتوب حرفياً:
```typescript
verification: { google: "YOUR_VERIFICATION_CODE" },  // ← لم يتم تعيينه
```

#### 🟡 مشكلة 4: لا يوجد فحص لإلزامية المتغيرات
لا يوجد أي كود في بداية التطبيق يتحقق من وجود المتغيرات الضرورية ويرمي خطأً واضحاً عند غيابها.

### 💡 الحل المقترح

**أ. ملفات البيئة الصحيحة:**
```
.env.local          ← التطوير المحلي (في .gitignore)
.env.production     ← الإنتاج (قيم مختلفة تماماً)
.env.example        ← نموذج للمطورين (بدون قيم حقيقية)
```

**ب. التحقق من المتغيرات عند البدء:**
```typescript
// src/lib/env.ts (ملف جديد)
const required = ['NEXT_PUBLIC_SUPABASE_URL', 'NEXT_PUBLIC_SUPABASE_ANON_KEY'];
required.forEach(key => {
  if (!process.env[key]) throw new Error(`Missing env var: ${key}`);
});
export const IS_MOCK = process.env.NEXT_PUBLIC_IS_MOCK_MODE === 'true';
```

**ج. `.env.example` الصحيح:**
```env
NEXT_PUBLIC_IS_MOCK_MODE=false          # ← false كافتراضي آمن
NEXT_PUBLIC_SUPABASE_URL=              # أضف URL مشروعك
NEXT_PUBLIC_SUPABASE_ANON_KEY=         # أضف مفتاحك
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

---

## 2. نظام المراقبة والسجلات (Monitoring & Logging)

### 🔍 تحليل الوضع الحالي

**ما يوجد حالياً:**

| النوع | الوضع |
|-------|-------|
| **أدوات المراقبة** (Sentry / Datadog) | ❌ لا يوجد |
| **تسجيل منظم** (Structured Logging) | ❌ لا يوجد |
| **مراقبة الأداء** | ❌ لا يوجد |
| **تتبع الجلسات** (Session Replay) | ❌ لا يوجد |
| **تنبيهات الأخطاء الحرجة** | ❌ لا يوجد |

**ما يوجد بدلاً منه:**

يعتمد المشروع على `console.error` فقط، **الذي لا يُسجَّل في أي مكان في الإنتاج**:

```typescript
// مثال من supabaseService.ts - يتكرر 15+ مرة
console.error('Error fetching profile:', error);
console.error('Error fetching properties:', error);
console.error('Error uploading image:', error);
// ...إلخ
```

**الأثر الفعلي:** أي خطأ يحدث للمستخدمين في الإنتاج **يختفي تماماً** — لا يعلم به المطوّر.

### ⚠️ المشكلات المكتشفة

#### 🔴 مشكلة 1: الأخطاء غير مرئية في الإنتاج
`console.error` يظهر في وحدة تحكم المتصفح فقط، ولا يُرسَل لأي خادم. إذا واجه مستخدم خطأ، لا توجد طريقة للمطوّر ليعلم بذلك.

#### 🔴 مشكلة 2: لا توجد مقاييس أداء
لا يوجد قياس لـ:
- زمن تحميل الصفحات
- سرعة استجابة Supabase
- معدل فشل العمليات
- عدد المستخدمين النشطين

#### 🟠 مشكلة 3: الرسائل غير منظمة وغير قابلة للفلترة
كل رسائل الخطأ نصوص عشوائية (`string`) دون مستوى (`level`)، سياق (`context`)، أو معرّف المستخدم (`userId`). من المستحيل فلترتها أو البحث فيها لاحقاً.

### 💡 الحل المقترح

**المرحلة 1: سجلات منظمة (قصيرة الأمد)**
```typescript
// src/lib/logger.ts (ملف جديد)
type LogLevel = 'info' | 'warn' | 'error';

export function log(level: LogLevel, message: string, context?: Record<string, unknown>) {
  const entry = {
    timestamp: new Date().toISOString(),
    level,
    message,
    ...context,
  };

  if (process.env.NODE_ENV === 'production') {
    // إرسال لـ Sentry / Datadog لاحقاً
    fetch('/api/logs', { method: 'POST', body: JSON.stringify(entry) });
  } else {
    console[level](`[${level.toUpperCase()}]`, message, context);
  }
}

// الاستخدام:
log('error', 'Error fetching profile', { userId, error: error.message });
```

**المرحلة 2: Sentry للإنتاج (موصى به بشدة)**
```bash
npm install @sentry/nextjs
npx @sentry/wizard@latest -i nextjs
```
يوفر مجاناً: تتبع الأخطاء، Stack Traces، تنبيهات بالإيميل، Session Replay.

**المرحلة 3: مراقبة الأداء**
- Next.js Analytics (مجاني على Vercel)
- Supabase Dashboard لمراقبة استعلامات قاعدة البيانات

---

## 3. معالجة الأخطاء (Error Boundaries & UX)

### 🔍 تحليل الوضع الحالي

**ما يوجد حالياً:**

| آلية | الوضع |
|------|-------|
| **`error.tsx`** في Next.js | ❌ **غير موجود** |
| **`not-found.tsx`** | ❌ **غير موجود** |
| **`global-error.tsx`** | ❌ **غير موجود** |
| **React Error Boundary** | ❌ **غير موجود** |
| **حالة الخطأ في الـ UI** | ⚠️ باستخدام `alert()` فقط |

**الأثر الحالي:**
```
خطأ غير متوقع → شاشة بيضاء فارغة / رسالة Next.js التقنية
                  المستخدم لا يعرف ماذا حدث
                  لا توجد طريقة للعودة أو المحاولة مجدداً
```

**نمط الأخطاء الحالي (يتكرر في 18+ صفحة):**
```typescript
// نمط موحد لكنه معيوب - يستخدم alert() بدلاً من UI
} catch (error) {
  console.error('Error adding property:', error);
  alert('حدث خطأ أثناء إضافة العقار. يرجى المحاولة مرة أخرى.');  // ❌
}
```

`alert()` مشكلة لأنها:
- تُحجب من بعض المتصفحات
- لا تتوافق مع الـ PWA على iOS
- تبدو غير احترافية
- لا تملك زر "إعادة المحاولة"

### ⚠️ المشكلات المكتشفة

#### 🔴 مشكلة 1: لا توجد صفحة خطأ عامة
إذا حدث خطأ جسيم في أي مكوّن، يرى المستخدم شاشة بيضاء أو رسالة Next.js التقنية الإنجليزية.

#### 🔴 مشكلة 2: لا توجد صفحة 404 محلية
إذا ذهب المستخدم لرابط غير موجود (مثل `/property/invalid-id`)، يرى الصفحة الافتراضية لـ Next.js دون توجيه للرئيسية أو الخيارات المناسبة.

#### 🟠 مشكلة 3: معالجة الأخطاء غير متسقة
بعض الأماكن تستخدم `alert()`، وبعضها يتجاهل الخطأ تماماً (`catch (error) { }` بدون أي فعل). لا يوجد نظام Toast مستخدم بشكل موحد رغم وجود `ToastProvider` في المشروع!

```typescript
// providers.tsx - ToastProvider موجود لكن غير مستخدم في الأخطاء!
<ToastProvider>
  {children}
</ToastProvider>
```

#### 🟡 مشكلة 4: لا توجد حالة "إعادة المحاولة"
عند فشل تحميل البيانات، لا يستطيع المستخدم إعادة المحاولة دون تحديث الصفحة بالكامل.

### 💡 الحل المقترح

**أ. الملفات المطلوبة (Next.js App Router):**

```
src/app/
├── error.tsx          ← خطأ على مستوى التطبيق مع زر إعادة المحاولة
├── not-found.tsx      ← صفحة 404 جميلة بروابط مفيدة
└── global-error.tsx   ← خطأ حرج (يستبدل layout أيضاً)
```

```tsx
// src/app/error.tsx (مثال)
'use client';
export default function Error({ error, reset }: { error: Error; reset: () => void }) {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen text-center p-8">
      <h2 className="text-2xl font-bold mb-4">حدث خطأ غير متوقع</h2>
      <p className="text-gray-500 mb-8">{error.message}</p>
      <button onClick={reset} className="btn-primary">إعادة المحاولة</button>
      <a href="/" className="mt-4 text-primary">العودة للرئيسية</a>
    </div>
  );
}
```

**ب. توحيد استخدام Toast بدلاً من `alert()`:**
```typescript
// استبدال كل alert() بـ:
import { useToast } from '@/components/ui/Toast';  // موجود بالفعل!

const { showToast } = useToast();
// بدلاً من: alert('حدث خطأ...')
showToast('حدث خطأ أثناء الحفظ. يرجى المحاولة مجدداً.', 'error');
```

---

## 📊 ملخص التقييم والأولويات

| الفئة | الوضع الحالي | الأولوية | الجهد المطلوب |
|-------|-------------|---------|--------------|
| **إدارة البيئة** | IS_MOCK_MODE=true في الإنتاج | 🔴 حرجة | 2 ساعة |
| **صفحة error.tsx** | غير موجودة | 🔴 حرجة | 1 ساعة |
| **صفحة not-found.tsx** | غير موجودة | 🔴 حرجة | 1 ساعة |
| **استبدال alert() بـ Toast** | 18+ موضع | 🟠 عالية | 3 ساعات |
| **تسجيل الأخطاء المنظم** | console.error فقط | 🟠 عالية | 4 ساعات |
| **تكامل Sentry** | غير موجود | 🟡 متوسطة | 2 ساعة |
| **مراقبة الأداء** | غير موجود | 🟡 متوسطة | 4 ساعات |
| **متغيرات بيئة منفصلة للإنتاج** | غير موجود | 🟡 متوسطة | 2 ساعة |

---

## ✅ خطة التنفيذ المقترحة

### المرحلة 1 (فورية – يوم واحد)
1. تغيير `NEXT_PUBLIC_IS_MOCK_MODE=false` في `.env.example`
2. إنشاء `src/app/error.tsx`
3. إنشاء `src/app/not-found.tsx`

### المرحلة 2 (قصيرة الأمد – أسبوع)
4. استبدال جميع `alert()` باستخدام `ToastProvider` الموجود
5. إنشاء `src/lib/logger.ts` لتوحيد السجلات
6. إضافة متغيرات البيئة الناقصة

### المرحلة 3 (متوسطة الأمد – شهر)
7. دمج Sentry لتتبع الأخطاء في الإنتاج
8. إعداد مشروعَي Supabase منفصلَين (تطوير وإنتاج)
9. إضافة مراقبة الأداء عبر Vercel Analytics

---

> **ملاحظة خاصة:** المشروع يمتلك بنية جيدة وكود نظيف في معظم المناطق، لكنه يفتقر للطبقات التشغيلية اللازمة لضمان الاستقرار في بيئة الإنتاج. التغييرات المطلوبة في المرحلة 1 بسيطة وسريعة، وتحقق تحسناً كبيراً في موثوقية التطبيق.
